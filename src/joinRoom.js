import React from "react";

/**
 * Join Room Button
 * Pulls up modal with form to join room
 * form takes name only (for now)
 *  see about showing list of active rooms? Would need to hold list in state
 * Button in form will redirect to new room
 */
function JoinRoom() {


  return (
      <button type="button" className="btn btn-primary">Join Group</button>
  );
}

export default JoinRoom;